package Game;

public class GameManager {

	public static Board board = new Board();

	public GameManager() {
		startGame();
	}
	
	// turn generator and board updater
	private void startGame() {
		/*
		 * player x (starting with white), it's your turn
		 * user inputs a move
		 * checks if move given is in list of possible moves
		 * if it is in the list, execute the user's move, update the board
		 * if not, do nothing
		 */
	}
	
	
}
